import { Button } from "@headlessui/react"

export const Signup = ()=>{
    return(<div>
        <Button className="rounded-lg bg-sky-400 py-2 px-4 text-sm font-bold text-sky-900 data-[hover]:bg-sky-500 data-[active]:bg-sky-700">SignUp</Button>
    </div>)
}